# Test / FYP release APK — uses debug keystore (no separate keys file needed).
# For Play Store later: generate release.keystore and replace signing in build.gradle.

STORE_FILE=debug.keystore
STORE_PASSWORD=android
KEY_ALIAS=androiddebugkey
KEY_PASSWORD=android
